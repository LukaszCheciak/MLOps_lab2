[project]
name = "MLOps_course_AGH_lab_02"
version = "1.0.0"
description = ""
readme = "README.md"

requires-python = "==3.11.*"

dependencies = [
    "docker>=7.1.0",
    "duckdb",
    "jupyter",
    "matplotlib",
    "pandas",
    "psycopg[binary]",
    "pyarrow",
    "ruff",
    "scipy",
]
